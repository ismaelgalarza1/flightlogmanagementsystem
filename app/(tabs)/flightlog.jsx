import { View, Text } from "react-native";

// the flightlog tab is going to have all funcationally of adding, editing and deleting flightlogs. it will have a modal where the operator can add thier information.

const FlightLog = () => {
  return (
    <View>
      <Text>Flight Log</Text>
    </View>
  );
};
export default FlightLog;
